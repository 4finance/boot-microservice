db:
  url: "jdbc:postgresql://localhost/db_name?ApplicationName=microservice_name"
  user: user
  password: password
  driver: org.postgresql.Driver

wiremock.port: 8030
microservice.config.file: "classpath:test-microservice.json"
city.finding.service.url: "http://localhost:${wiremock.port}"

graphite.host: localhost